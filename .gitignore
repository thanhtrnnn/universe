# Credentials / ghi chú riêng — KHÔNG commit
NOTE.md
*.env
.env

# Agents
.rtk/
CLAUDE.md

universe-desktop/dist

# DB viewer - công cụ dev cục bộ (đường dẫn psql theo từng máy) -> không commit.
# Giữ README.md để vẫn có hướng dẫn trong repo.
tools/db-viewer/*