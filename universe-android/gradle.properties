org.gradle.jvmargs=-Xmx1024m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# Backend chạy trên Render (HTTPS) — dùng được cả emulator lẫn điện thoại thật,
# không cần cùng mạng LAN với máy dev.
UNIVERSE_API_BASE_URL=https://universe-api-sgp7.onrender.com/api/
